# HANDOFF — Ritual B2B Parser — 2026-06-01

## Состояние (обновлено 2026-06-01, после деплоя)

Клон crimea-парсера → ritual готов на ветке **`feat/ritual-clone`**, **pytest 53 green**,
**задеплоен на боевой сервер** `212.116.115.150` (box-891610) и работает:
- парсер в `/home/ritual_parser`, бот в `/opt/ritual_admin_bot` (`active`)
- systemd-юниты `ritual_*`; таймер парсера — **пн 03:00 МСК** (crimea — вс, не пересекаются); watchdog 10 мин
- secrets в `.env`: VK_TOKEN/GDRIVE подтянулись из crimea автоматически; TG_BOT_TOKEN/TG_CHAT_ID/GDRIVE_FOLDER_ID — заказчика
- бот: **@parsing_ritualb2b_bot**, группа «Парсинг_ритуалка_чат» (`TG_CHAT_ID=-5101031772`, он же в `ADMIN_CHAT_IDS`)
- тестовое сообщение в группу доставлено; pytest на сервере 53 green
- **прогон парсинга ещё НЕ запускали** — ждёт проверки бота / ручного `/run` / таймера в пн

Деплой выполнялся через `hotels_sbor_baza/_deploy_helper.py` (paramiko SSH, креды внутри).
SSH к проду гейтит auto-classifier — разблокируется Bash-правилом в `.claude/settings.local.json` (gitignored).

### Исправлено на живом тесте (баги, унаследованные от клона / внесённые при адаптации)
- `/start` падал: буквальный `<name>` в HTML-тексте → `&lt;name&gt;` (`commands.py`)
- кнопки меню молчали в группе: callback проверял `from_user.id` (личный id) вместо `chat.id` (id группы)
  → `is_admin(cb.message.chat.id)` (`menu.py`)
- **VK=0 и Telegram 404**: `env_loader` через `glob(.env.*)` с `override=True` грузил `.env.*.example`
  (с крымскими/плейсхолдер-значениями) поверх реального `.env`. Фикс: игнорировать `.example/.sample/.template`;
  на сервере удалены `.env.*.example`; `.env` дозаполнен (в т.ч. `VK_TOKEN` дописан строкой — в `.env.example` её не было).
- **VK падал на классификации** `normalize() takes 1 arg but 2`: при PowerShell-правке импортов alias
  `as vk_classify` прилип к `normalize`. Фикс: `from parsers.vk_filter import classify as vk_classify` +
  `from utils.categories import normalize` (`vk_groups.py`).
- **VK `database is locked`** при параллельной записи с основным прогоном: `dedup.py` → `busy_timeout=60000`,
  `timeout=60`.

VK-добор после фиксов собирает группы (≈2364 по городам) и пишет; запускается как `ritual_vk.service`
(`systemd-run`, HTTP, можно параллельно основному прогону).

## Что сделано

- Плоский клон `hotels_sbor_baza/_extracted/crimea_parser/` + `parser_admin_bot/` → `ritual_admin_bot/`
- Удалены отельные парсеры (avito/sutochno/ostrovok/twogis/gosreestr) и бот №1 (`bot/`)
- Инфраструктура: `crimea_*`→`ritual_*`, путь бота `/opt/ritual_admin_bot/`, таймер **Mon 03:00**
- Классификация: `categories` (client_type ритуала), `vk_filter` (ритуал/флористика/noise/ambiguous)
- Гео: +19 городов ЗП/Хс (под контролем РФ); **спорная зона** (Запорожье/Херсон/правобережье) **исключена**
- Источники: osm (funeral-теги, bbox 4 региона), wikidata (Q-коды/регионы), wikipedia (категории),
  yandex/search (ритуальные запросы), crawler (RITUAL/FLORIST_TRIGGERS), vk_groups (RITUAL_QUERIES + client_type)
- `email_finder.extract_socials` (tg/inst/wa/ok) + WhatsApp→phone
- excel: вкладки «Все ритуальные» / «Флористика» / «Требуют проверки», палитра по client_type
- Скрипты: `fetch_vk_cities` (новый), `cleanup_classification` (адаптация cleanup_vk)
- Тесты: `test_ritual_keywords`, `test_extract_socials`, +города ЗП/Хс в `test_geo_city`

## Что осталось

1. **Проверка бота заказчиком**: `/start` + кнопка «Статус» в группе (после фиксов должны работать).
   Если захочет управлять из лички — добавить его личный user_id в `ADMIN_CHAT_IDS` бота `.env`.
2. **Первый сбор**: либо ручной `/run` (или `/run_source vk` по одному городу для пробы),
   либо ждать авто-таймер в понедельник 03:00.
3. **VK city_id для ЗП/Хс**: на сервере `cd /home/ritual_parser && venv/bin/python scripts/fetch_vk_cities.py`
   → вставить вывод в `parsers/vk_groups.py:VK_CITIES` (сейчас только крымские; ЗП/Хс пока соберут OSM/Я.Карты/Поиск).
4. **Перепроверить Wikidata Q-коды** (spec 3.6) — типы funeral могут вернуть 0; источник второстепенный.
5. **crimea_parser.service в состоянии `failed`** — отдельный отельный проект, ritual не задевает; глянуть при случае.
6. После приёмки — оценить качество (spec §7.3) и докрутить словари `vk_filter`/`categories` по факту.

## Известные нюансы

- Я.Карты/2ГИС могут блокировать новые регионы (ЗП/Хс) как Крым → fallback на VK + Поиск.
- В части docstring/комментариев `parsers/` ещё встречается «отель/hotel» — косметика, на работу не влияет.
- `deploy.sh` генерит юниты heredoc'ом (parser/watchdog/email_finder/source@); файлами в `deploy/` —
  только `ritual_archive.{service,timer}` + logrotate.

## Приёмка (spec §7.3)

Приоритет — **качество классификации** (≥90% верных на 50 случайных записях из «Все ритуальные»),
объём ≥800 уникальных — ориентир, не блокер.
