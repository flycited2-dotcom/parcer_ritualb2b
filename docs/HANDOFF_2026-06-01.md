# HANDOFF — Ritual B2B Parser — 2026-06-01

## Состояние

Клон crimea-парсера → ritual выполнен на ветке **`feat/ritual-clone`**. Код адаптирован,
**pytest 53 green**, весь pipeline (`main.py` + все `parsers`) импортируется. **НЕ задеплоен.**

Коммиты ветки:
- `docs(design)` — дизайн + ревизия spec
- `feat(clone)` — плоский клон + инфра-переименование
- `feat(ritual)` — адаптация словарей/гео/источников/excel/соцсетей/тестов

## Что сделано

- Плоский клон `hotels_sbor_baza/_extracted/crimea_parser/` + `parser_admin_bot/` → `ritual_admin_bot/`
- Удалены отельные парсеры (avito/sutochno/ostrovok/twogis/gosreestr) и бот №1 (`bot/`)
- Инфраструктура: `crimea_*`→`ritual_*`, путь бота `/opt/ritual_admin_bot/`, таймер **Mon 03:00**
- Классификация: `categories` (client_type ритуала), `vk_filter` (ритуал/флористика/noise/ambiguous)
- Гео: +19 городов ЗП/Хс (под контролем РФ); **спорная зона** (Запорожье/Херсон/правобережье) **исключена**
- Источники: osm (funeral-теги, bbox 4 региона), wikidata (Q-коды/регионы), wikipedia (категории),
  yandex/search (ритуальные запросы), crawler (RITUAL/FLORIST_TRIGGERS), vk_groups (RITUAL_QUERIES + client_type)
- `email_finder.extract_socials` (tg/inst/wa/ok) + WhatsApp→phone
- excel: вкладки «Все ритуальные» / «Флористика» / «Требуют проверки», палитра по client_type
- Скрипты: `fetch_vk_cities` (новый), `cleanup_classification` (адаптация cleanup_vk)
- Тесты: `test_ritual_keywords`, `test_extract_socials`, +города ЗП/Хс в `test_geo_city`

## Что осталось (перед/при деплое)

1. **VK city_id**: `python scripts/fetch_vk_cities.py` (нужен `VK_TOKEN`) → вставить результат
   в `parsers/vk_groups.py:VK_CITIES` (сейчас там только крымские; без этого VK не соберёт ЗП/Хс).
2. **Секреты в `.env`** на сервере:
   - `VK_TOKEN`, `GDRIVE_TOKEN`, `GDRIVE_CREDENTIALS` подтягиваются **автоматически** из
     `/home/crimea_parser/.env` при `bash deploy.sh` (тот же приём, что в парсере монтажников).
   - Вручную заполнить только ritual-специфичные: `TG_BOT_TOKEN`, `TG_CHAT_ID`, `GDRIVE_FOLDER_ID`
     (получены от заказчика, в local memory прошлой сессии).
   - Реального `VK_TOKEN` нет ни в одном репозитории (он только на сервере) — это правильно.
3. **Перепроверить Wikidata Q-коды** (spec 3.6) — типы funeral могут вернуть 0; источник второстепенный.
4. **Деплой**: смержить ветку → на сервере `bash deploy.sh` + `ritual_admin_bot/install.sh`;
   `sed -i 's/\r$//'` на `.sh` (CRLF из Windows); smoke-test по spec §7.2.

## Известные нюансы

- Я.Карты/2ГИС могут блокировать новые регионы (ЗП/Хс) как Крым → fallback на VK + Поиск.
- В части docstring/комментариев `parsers/` ещё встречается «отель/hotel» — косметика, на работу не влияет.
- `deploy.sh` генерит юниты heredoc'ом (parser/watchdog/email_finder/source@); файлами в `deploy/` —
  только `ritual_archive.{service,timer}` + logrotate.

## Приёмка (spec §7.3)

Приоритет — **качество классификации** (≥90% верных на 50 случайных записях из «Все ритуальные»),
объём ≥800 уникальных — ориентир, не блокер.
